# TheEvents UAE

Official baseline website structure.

## Setup
1. Copy `.env.example` to `.env.local`
2. Add Supabase URL/key
3. Run `npm install`
4. Run `npm run dev`

## Deploy
Push to GitHub, import into Vercel, add env vars, connect domain.

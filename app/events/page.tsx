import { Header } from "@/components/Header"; import { EventCard } from "@/components/EventCard"; import { demoEvents } from "@/lib/data";
export default function EventsPage(){return <main className="min-h-screen bg-[#FFF9FD] text-[#2D2638]"><Header/><section className="mx-auto max-w-7xl px-6 py-10"><h1 className="text-5xl font-black">Events</h1><p className="mt-3 text-[#74687D]">Browse UAE events and exhibition booth opportunities.</p><div className="mt-8 grid gap-6 md:grid-cols-3">{demoEvents.map((event)=><EventCard key={event.id} event={event}/>)}</div></section></main>}
